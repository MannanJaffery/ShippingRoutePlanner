# ShippingRoutePlanner
A C++ project for planning optimal shipping routes using Dijkstra’s algorithm. Includes data structures (LinkedList, Queue, MinHeap, Graph), models for ports &amp; routes, user preferences, layover management, and a visualizer using SFML. Ideal for logistics simulation and route optimization.
